# 深色模式、批量导入与自然交流验收

日期：2026-09-19。测试资料全部虚构。前端 8765、真实后端 8766；Playwright 直接操作网页，没有替换接口响应。

## 自动化

- 后端：26 passed；两条已有依赖弃用警告。
- 前端：TypeScript + Vite production build 通过。
- 新覆盖：三种导入失败不留空候选人/文件、两人简历隔离、开场不调用模型、不更新声明、不进入后续专业历史；回答引用修正及最多两次调用。

## 网页结果

项目 #5“多候选人工作台演示（虚构）”，候选人林初 #5、周宁 #6、许然 #7。通过同一次文件选择上传 TXT、DOCX、PDF 以及损坏 PDF；成功 3 / 4，失败 1。重试仅处理失败文件，候选人数仍为 3，没有多余空档案。姓名可在导入前改写。文件只做本地解析。

林初与周宁的独立档案切换正常，各自只显示对应文件；查找“林初”只显示一张卡片。最近面试入口指向其会话。

深色模式显示正常；切到浅色后刷新保留浅色，切回深色后刷新保留深色。390px 宽度无横向溢出。截图位于 output/playwright/dark-candidates.png、dark-opening.png、mobile-dark-session.png，不提交 Git。

真实 DeepSeek 生成提纲 #6，采用自然交流，3 道专业题包含经历与未来情境。会话 #4 实测 2 轮（开场 + 一道专业题，刻意缩短验收，不代表完整评估）。开场只保存，专业题真实分析成功，最终 completed 2/2。候选人均为虚构；此测试不能证明招聘有效性。

## 实际发现与修复

首次专业分析把回答内容误用为简历引文，返回 502，页面保留回答，数据库未新增该轮。补充正确简历上下文和一次有上限的 JSON/引用修正后，用同一虚构回答重新提交成功。未通过放宽原文校验规避错误。

浏览器控制台中两条 422 来自故意损坏 PDF 的首次及重试，另有一条上述修复前的 502；这些是实际失败记录，不表述为全过程零错误。最终流程无未处理的前端运行异常。

## 仍需进一步验证

自然语气仍由模型生成，不保证每题都足够简短；需真人面试官试用。默认开场可用且可跳过，但计时、收尾/反问、专业题编辑替换仍未做。批量跨刷新续传、跨批去重和请求幂等 token 未做；网络响应不确定时先查已导入人员。
