# 第一阶段验收记录

验证环境：Windows、Python 3.14.5、Node.js 24.15.0。

- FastAPI 在 127.0.0.1:8766 启动成功；React / Vite 在 127.0.0.1:8765 启动成功。
- TypeScript 检查与 Vite production build 通过。
- pytest：3 个集成测试通过，覆盖三种文件类型、TXT 编码、SQLite 重开、外键记录校验、空输入、损坏文件、20 MB 限制、清理、无文本 PDF、密钥隐藏 / 保留 / 清除。依赖内部有两条弃用警告，不影响执行。
- Playwright 实际浏览器创建项目和候选人，分别上传 TXT、DOCX、PDF，检查提取的 Python 关键字；刷新页面后再次点击查看解析文本成功。
- 设置页配置保存、刷新读取成功；验收配置已恢复为空，无真实 API Key。
- 实际停止后端进程并重新启动，通过 8765 的代理验证项目、候选人、三份简历文本及设置仍然存在。
- 岗位 / JD 创建页可以打开。
- 已检查候选人页面截图；已修复 favicon 404。修复后页面导航没有新增控制台错误。
- Git 忽略规则覆盖数据库、简历、环境配置、依赖目录和测试输出。

本地 data 中保留“MVP 验收示例”项目、“测试候选人”和三份合成简历，便于直接查看。它们不进入 Git。未调用任何 AI API。

已修复：Windows 下损坏 PDF 的解析异常会保留底层文件句柄，妨碍删除；改为从受大小限制的文件字节解析，验证错误响应与失败清理正常。
