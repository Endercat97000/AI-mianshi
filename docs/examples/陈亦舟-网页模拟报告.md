# 面试证据报告

会话：3 | 状态：completed | 已回答：5/5

模型：deepseek / deepseek-flash | 提示词：2026-09-18.2 | 提纲 #5 | 简历 #6

模型分析仅为待复核线索；SUPPORTED 不代表经历已被外部证实。本报告不作录用决定。

## 能力覆盖

- Python FastAPI 接口开发：0 轮

- 简历文件解析与异常处理：2 轮

- SQLite 数据持久化：1 轮

- 大模型 API 接入与超时处理：1 轮

- 本地部署与交付：1 轮

- 测试与质量保障：0 轮

- 需求理解与实现取舍：0 轮

## 提纲选定的 Skill 快照

- project-requirement · 53fff88a6dcf65d9

- question-design · 0d78d967f76bd825

- competition-analysis · 0d5faa7dbf92dc09

- roles-backend · b05f9195a7dc80de

## 竞赛参考价值

仅依据简历自述；未联网核实官方规则及获奖记录。不是绝对含金量评分。

### 青禾校园软件创新赛

简历原文：2022 年青禾校园软件创新赛，校级团队二等奖，三人团队。

届次 / 赛道：2022 年，校级，赛道未知

奖项层级：校级团队二等奖

赛制：团队赛，三人团队

竞争强度：未知，待查当届官方规则；简历未提供参赛队伍数量、获奖比例或晋级规则

岗位相关性：作品为设备借用原型，候选人负责 Python 借还接口和数据库，与后端接口和数据库能力有一定关联；不能证明生产环境部署、运维或交付能力。

个人贡献：候选人自述负责 Python 借还接口和数据库，队友负责界面及演示；未提供可复核的代码、作品或证书。

参考价值：对后端接口和数据库实践有补充参考，工程交付能力仍需项目证据；赛事竞争强度未知，不作为录用依据。

待确认：主办方规则未知；参赛队伍数量未知；获奖比例未知；官方名单或证书未提供；赛道和赛区未知

## 声明与原文

### 声明 1：候选人自述负责文件保存、正文解析及大模型摘要 API 接入。

模型状态：SUPPORTED

简历原文（字符 204–239）：

本人负责文件保存、正文解析及大模型摘要 API 接入；检索由同事负责。

待确认：请候选人说明解析 PDF/DOCX 的具体库、流程和失败判断方式，并区分本人实现与同事负责的部分。

回答证据（记录 #4）：我当时用 PyMuPDF 打开 PDF，逐页取文字后合并。上传时先检查类型和大小，再保存文件，解析结果写入数据库。

分析：候选人说明了 PDF 解析的具体库、逐页取文字合并的流程，以及上传时先检查类型和大小再保存、解析结果写入数据库，与其自述负责文件保存和正文解析一致。该状态仅表示本轮回答提供了线索，不代表外部事实核实。

回答证据（记录 #5）：有一份双栏 PDF，直接提取后左栏一句后面接了右栏一句，摘要把两段经历串在了一起。我是拿原页面和提取文本逐行对照才发现的。

分析：候选人给出了正文解析中阅读顺序错乱的具体案例和发现方式，与其自述负责正文解析一致，为本条自述提供了线索；该状态不代表外部事实核实。

回答证据（记录 #6）：我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。

分析：本轮回答聚焦大模型摘要 API 的超时处理，未涉及文件保存、正文解析的具体实现或与同事的分工，无法据此更新该条关于文件保存与正文解析职责的自述。

回答证据（记录 #7）：我设计了设备表和借用记录表，设备有编号和可借状态，借用记录有设备、借用人、借出时间和归还时间。

分析：本轮回答聚焦设备借还接口的表结构与并发处理，未涉及文件保存、正文解析的具体实现或与同事的分工，无法据此更新该条关于文件保存与正文解析职责的自述。

回答证据（记录 #8）：我的方案是前端预先构建，随程序交付；后端只监听本机，提供启动和停止入口，启动成功再打开浏览器。

分析：本轮回答聚焦本地部署与交付方案，未涉及文件保存、正文解析的具体实现或与同事的分工，无法据此更新该条关于文件保存与正文解析职责的自述。

### 声明 2：候选人自述处理过可打开但无法提取文字的 PDF 以及模型调用超时，没有实现 OCR。

模型状态：SUPPORTED

简历原文（字符 240–277）：

处理过可打开但无法提取文字的 PDF，以及模型调用超时。没有实现 OCR。

待确认：请候选人描述一次具体超时或解析失败案例的触发条件、处理动作和最终结果，确认其实际参与程度。

回答证据（记录 #4）：遇到能打开但没有文字的文件，我最初也以为是坏文件，后来检查页面才发现有些是图片扫描件。我们没有做 OCR，所以最后提示没有提取到文字，请换文字版或者粘贴正文；不能直接断言一定是扫描件。损坏文件打不开则另报解析失败。

分析：候选人具体描述了可打开但无法提取文字的 PDF 的判断和处理方式，并区分了损坏文件打不开的情况，支持其自述处理过此类解析异常；但本轮回答未涉及模型调用超时的具体案例，因此对该条自述只提供部分线索。

回答证据（记录 #5）：后来试过按文本块坐标排序，对那份样本有改善，但带跨栏标题的文件仍会错，所以没有把它当通用解决方案上线。我只加了解析文本预览让用户检查，完整的版面测试集没来得及做。

分析：本轮回答补充了阅读顺序问题的改进尝试与边界（坐标排序仅对样本有改善、跨栏标题仍错、未上线、未做完整版面测试集），属于解析异常处理范畴的线索；但未涉及模型调用超时的具体案例，因此对该条自述仅部分支持。

回答证据（记录 #6）：当时有一份比较长的资料，摘要请求一直没有返回，页面就一直转圈。我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。第一次做得比较简单，超时就返回可重试的错误，保留原文件和解析文本，不把空字符串存成成功摘要。我没有直接无限重试，后来对限流和临时服务错误最多重试一次。

分析：候选人给出了模型调用超时的具体场景（长资料摘要请求未返回、页面转圈）、超时设置（连接约 5 秒、读取约 40 秒，并说明记不准）、重试边界（不无限重试，限流和临时服务错误最多重试一次）以及失败处理（返回可重试错误、保留原文件和解析文本、不把空字符串存成成功摘要），为其自述处理过模型调用超时提供了直接线索。该状态仅表示本轮回答提供了线索，不代表外部事实核实。

回答证据（记录 #7）：原来的实现是先查可借再写记录，现在想起来这在并发下不可靠；20 人使用时没遇到，不代表没有问题。

分析：本轮回答未涉及可打开但无法提取文字的 PDF 或模型调用超时的具体案例，无法更新该条关于解析异常与超时处理的自述。

回答证据（记录 #8）：备份要同时考虑数据库和原文件，第一版可以先要求关闭程序后备份，并实际恢复一次。

分析：本轮回答提到备份与恢复，但未涉及可打开但无法提取文字的 PDF 或模型调用超时的具体案例，无法更新该条关于解析异常与超时处理的自述。

### 声明 3：候选人自述上传重复提交问题由前端禁用按钮缓解，尚未完整实现服务端幂等。

模型状态：PARTIALLY_SUPPORTED

简历原文（字符 278–308）：

上传重复提交问题由前端禁用按钮缓解，尚未完整实现服务端幂等。

待确认：请候选人说明为何选择前端缓解而非服务端幂等，以及该取舍带来的风险和后续改进计划。

回答证据（记录 #4）：复杂表格的阅读顺序我当时没做好，只做了几份常见文件的人工检查。

分析：本轮回答未涉及上传重复提交、前端禁用按钮或服务端幂等，无法据此更新该条关于需求理解与实现取舍的自述。

回答证据（记录 #5）：我只加了解析文本预览让用户检查，完整的版面测试集没来得及做。

分析：本轮回答未涉及上传重复提交、前端禁用按钮或服务端幂等，无法据此更新该条关于需求理解与实现取舍的自述。

回答证据（记录 #6）：用户再次点摘要可能重复花费，这个服务端幂等当时没做完整，是已知欠缺。

分析：本轮回答主动承认服务端幂等未做完整、用户重复点击摘要可能重复花费，与其自述“尚未完整实现服务端幂等”一致，为需求理解与实现取舍提供了线索；但未说明为何选择该取舍、前端缓解方式及后续改进计划，因此仅部分支持。

回答证据（记录 #7）：如果改进，我会在事务里做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任何一步失败都回滚。这样两个请求不能都成功。

分析：候选人主动指出原实现先查后写在并发下不可靠，并提出事务内带条件更新、检查受影响行数、失败回滚的改进方案，属于对并发一致性的取舍判断，为需求理解与实现取舍提供了线索；但未说明为何此前选择前端缓解而非服务端幂等，也未给出后续落地计划，因此仅部分支持。

回答证据（记录 #8）：我还会让面试官试操作：如果他没时间逐字记回答，就要区分他的摘要和候选人原话，报告不能把摘要伪装成逐字引用。

分析：候选人提出让面试官试操作，并强调区分面试官摘要与候选人原话、报告不能把摘要伪装成逐字引用，体现了对使用流程与数据呈现的取舍判断，为需求理解与实现取舍提供了线索；但未涉及上传重复提交、前端禁用按钮或服务端幂等，因此仅部分支持。

### 声明 4：候选人自述使用 FastAPI 和 SQLite 为约 20 人团队开发设备借还接口。

模型状态：SUPPORTED

简历原文（字符 323–361）：

使用 FastAPI 和 SQLite，为约 20 人团队开发设备借还接口。

待确认：请候选人说明借还接口的表结构、并发借出时的处理方式以及 SQLite 在该场景下的限制。

回答证据（记录 #4）：上传时先检查类型和大小，再保存文件，解析结果写入数据库。

分析：本轮回答仅提到解析结果写入数据库，未涉及设备借还接口、表结构或并发处理，无法更新该条关于 SQLite 数据持久化的自述。

回答证据（记录 #5）：如果现在重做，我会先收集获得授权的几类样本，再比较提取结果，不会声称换个库就全部解决。

分析：本轮回答未涉及设备借还接口、表结构或并发处理，无法更新该条关于 SQLite 数据持久化的自述。

回答证据（记录 #6）：对鉴权错误不会重试，也不会把上游完整错误和密钥展示给用户。

分析：本轮回答未涉及设备借还接口、表结构或并发处理，无法据此更新该条关于 SQLite 数据持久化的自述。

回答证据（记录 #7）：我设计了设备表和借用记录表，设备有编号和可借状态，借用记录有设备、借用人、借出时间和归还时间。原来的实现是先查可借再写记录，现在想起来这在并发下不可靠；20 人使用时没遇到，不代表没有问题。如果改进，我会在事务里做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任何一步失败都回滚。这样两个请求不能都成功。

分析：候选人给出了设备借还接口的表结构（设备表含编号和可借状态，借用记录表含设备、借用人、借出时间和归还时间），并说明原实现先查可借再写记录、在并发下不可靠，以及事务内带条件更新加受影响行数检查的改进思路，为其自述使用 FastAPI 和 SQLite 开发设备借还接口提供了直接线索。该状态仅表示本轮回答提供了线索，不代表外部事实核实。

回答证据（记录 #8）：备份要同时考虑数据库和原文件，第一版可以先要求关闭程序后备份，并实际恢复一次。

分析：本轮回答提到备份需同时考虑数据库和原文件，但未涉及设备借还接口、表结构或并发处理，无法更新该条关于 SQLite 数据持久化的自述。

### 声明 5：候选人自述编写基础 pytest 接口测试和数据导出脚本，做过少量 React 表单。

模型状态：UNKNOWN

简历原文（字符 362–400）：

编写基础 pytest 接口测试和数据导出脚本，做过少量 React 表单。

待确认：请候选人说明 pytest 测试覆盖了哪些接口和异常场景，以及测试是否在持续集成或本地流程中运行。

回答证据（记录 #4）：复杂表格的阅读顺序我当时没做好，只做了几份常见文件的人工检查。

分析：本轮回答提到对几份常见文件做人工检查，但未涉及 pytest 接口测试的覆盖范围或运行方式，无法更新该条关于测试与质量保障的自述。

回答证据（记录 #5）：完整的版面测试集没来得及做。

分析：本轮回答提到未完成完整版面测试集，但未涉及 pytest 接口测试的覆盖范围或运行方式，无法更新该条关于测试与质量保障的自述。

回答证据（记录 #6）：第一次做得比较简单，超时就返回可重试的错误，保留原文件和解析文本，不把空字符串存成成功摘要。

分析：本轮回答未涉及 pytest 接口测试的覆盖范围或运行方式，无法更新该条关于测试与质量保障的自述。

回答证据（记录 #7）：我还没实际写过这个并发测试，需要用两个独立连接同时发请求验证。

分析：本轮回答提到尚未编写并发测试、需用两个独立连接同时发请求验证，但未涉及 pytest 接口测试的覆盖范围或运行方式，无法更新该条关于测试与质量保障的自述。

回答证据（记录 #8）：会先在一台没有开发环境的电脑做最小启动验证，再承诺时间。

分析：本轮回答提到在无开发环境电脑做最小启动验证，但未涉及 pytest 接口测试的覆盖范围或运行方式，无法更新该条关于测试与质量保障的自述。

### 声明 6：候选人自述未独立负责 Windows 安装包制作，没有大型系统部署经验。

模型状态：SUPPORTED

简历原文（字符 401–432）：

未独立负责 Windows 安装包制作，没有大型系统部署经验。

待确认：请候选人说明参与过的部署环节、使用的工具和遇到的困难，确认其可迁移的交付能力。

回答证据（记录 #4）：损坏文件打不开则另报解析失败。

分析：本轮回答未涉及 Windows 安装包制作、部署或交付环节，无法更新该条关于本地部署与交付的自述。

回答证据（记录 #5）：所以没有把它当通用解决方案上线。

分析：本轮回答未涉及 Windows 安装包制作、部署或交付环节，无法更新该条关于本地部署与交付的自述。

回答证据（记录 #6）：我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。

分析：本轮回答未涉及 Windows 安装包制作、部署或交付环节，无法更新该条关于本地部署与交付的自述。

回答证据（记录 #7）：竞赛原型也是较简单的版本，不能用那个奖项证明我已经解决了生产并发。

分析：本轮回答未涉及 Windows 安装包制作、部署或交付环节，无法更新该条关于本地部署与交付的自述。

回答证据（记录 #8）：我没有独立做过 Windows 安装包，会先在一台没有开发环境的电脑做最小启动验证，再承诺时间。我的方案是前端预先构建，随程序交付；后端只监听本机，提供启动和停止入口，启动成功再打开浏览器。程序、数据、日志分目录，甲方在设置页填自己的 Key，不把我的密钥或测试简历带过去。备份要同时考虑数据库和原文件，第一版可以先要求关闭程序后备份，并实际恢复一次。升级前保留备份。

分析：候选人明确重申未独立做过 Windows 安装包，并给出可迁移的交付思路：无开发环境电脑做最小启动验证、前端预构建随程序交付、后端仅监听本机并提供启动停止入口、程序/数据/日志分目录、甲方自填 Key、备份覆盖数据库与原文件并实际恢复一次、升级前保留备份，为其自述的部署与交付现状及思路提供了直接线索。该状态仅表示本轮回答提供了线索，不代表外部事实核实。

### 声明 7：候选人自述技能包括 Python、FastAPI、SQL、pytest，了解 React、TypeScript、OpenAI-compatible API。

模型状态：PARTIALLY_SUPPORTED

简历原文（字符 594–665）：

技能：Python、FastAPI、SQL、pytest；了解 React、TypeScript、OpenAI-compatible API。

待确认：请候选人结合项目说明 FastAPI 接口设计、依赖注入和错误处理的实际使用情况，避免仅以技能列表判断。

回答证据（记录 #4）：我当时用 PyMuPDF 打开 PDF，逐页取文字后合并。

分析：候选人结合项目说明了使用 PyMuPDF 进行 PDF 解析的实际使用情况，为其自述技能包括 Python 提供了线索；但本轮回答未涉及 FastAPI 接口设计、依赖注入或错误处理，因此仅部分支持技能列表中的相关声明。该状态不代表外部事实核实。

回答证据（记录 #5）：后来试过按文本块坐标排序，对那份样本有改善，但带跨栏标题的文件仍会错，所以没有把它当通用解决方案上线。

分析：本轮回答未涉及 FastAPI 接口设计、依赖注入或错误处理，无法据此更新该条关于 Python/FastAPI 技能的自述。

回答证据（记录 #6）：我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。

分析：候选人结合项目说明了在后端补充连接超时和读取超时的实际使用情况，为其自述技能包括 Python 及后端接口开发提供了线索；但本轮回答未涉及 FastAPI 接口设计、依赖注入或错误处理框架层面的实现，因此仅部分支持该技能声明。该状态不代表外部事实核实。

回答证据（记录 #7）：如果改进，我会在事务里做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任何一步失败都回滚。

分析：候选人结合设备借还接口说明了事务内带条件更新、受影响行数检查和回滚的实现思路，为其自述技能包括 Python、FastAPI、SQL 提供了线索；但本轮回答未涉及 FastAPI 接口设计、依赖注入或错误处理框架层面的实现，因此仅部分支持该技能声明。该状态不代表外部事实核实。

回答证据（记录 #8）：后端只监听本机，提供启动和停止入口，启动成功再打开浏览器。程序、数据、日志分目录，甲方在设置页填自己的 Key，不把我的密钥或测试简历带过去。

分析：候选人结合交付场景说明了后端仅监听本机、提供启动停止入口、目录分离和配置由甲方在设置页填写 Key 的做法，为其自述技能包括 Python、FastAPI 提供了线索；但本轮回答未涉及 FastAPI 接口设计、依赖注入或错误处理框架层面的实现，因此仅部分支持该技能声明。该状态不代表外部事实核实。

## 待确认清单

- 候选人自述 2023.07—2026.06 在青禾软件工作室工作，但简历未说明当前是否仍在职或离职时间。 / 原文：2023.07—2026.06 青禾软件工作室（虚构），后端开发工程师。 / 核实：请候选人确认该段工作经历的起止时间是否准确，以及当前在职状态。

- 候选人自述上传重复提交问题由前端禁用按钮缓解，尚未完整实现服务端幂等。 / 原文：上传重复提交问题由前端禁用按钮缓解，尚未完整实现服务端幂等。 / 核实：请候选人说明该问题是否影响过实际使用，以及后续是否有服务端幂等的改进计划。

- 候选人自述未独立负责 Windows 安装包制作，没有大型系统部署经验。 / 原文：未独立负责 Windows 安装包制作，没有大型系统部署经验。 / 核实：请候选人说明参与过的部署环节和工具，确认其可迁移的本地交付能力。

- 竞赛经历为虚构演示素材，未提供主办方规则、参赛队伍数量、获奖比例、官方名单或证书。 / 原文：未提供主办方规则、参赛队伍数量、获奖比例、官方名单或证书。 / 核实：请候选人说明该竞赛的赛道、参赛规模和本人具体贡献，仅作为补充参考，不作为录用依据。

## 面试记录

### 第 1 题（记录 #4）

你在内部资料检索工具中负责文件保存、正文解析及大模型摘要 API 接入。请具体讲一下 PDF 正文解析的实现方案，以及遇到可打开但无法提取文字的 PDF 时你是怎么判断和处理的？

回答：

【虚构候选人陈亦舟的模拟回答】我当时用 PyMuPDF 打开 PDF，逐页取文字后合并。上传时先检查类型和大小，再保存文件，解析结果写入数据库。遇到能打开但没有文字的文件，我最初也以为是坏文件，后来检查页面才发现有些是图片扫描件。我们没有做 OCR，所以最后提示没有提取到文字，请换文字版或者粘贴正文；不能直接断言一定是扫描件。损坏文件打不开则另报解析失败。复杂表格的阅读顺序我当时没做好，只做了几份常见文件的人工检查。

模型观察：

候选人本轮围绕 PDF 正文解析给出了较具体的实现线索：使用 PyMuPDF 逐页取文字后合并；上传时先检查类型和大小再保存文件，解析结果写入数据库；对可打开但无文字的 PDF，先误判为坏文件，后检查页面发现可能是图片扫描件，因未做 OCR 而提示未提取到文字并建议换文字版或粘贴正文，同时明确不能直接断言一定是扫描件；损坏文件打不开则另报解析失败；并主动承认复杂表格阅读顺序未做好，只做了几份常见文件的人工检查。这些内容对“简历文件解析与异常处理”能力提供了直接线索，但仍是候选人自述，未经过外部验证。

本轮引用：

我当时用 PyMuPDF 打开 PDF，逐页取文字后合并。上传时先检查类型和大小，再保存文件，解析结果写入数据库。遇到能打开但没有文字的文件，我最初也以为是坏文件，后来检查页面才发现有些是图片扫描件。我们没有做 OCR，所以最后提示没有提取到文字，请换文字版或者粘贴正文；不能直接断言一定是扫描件。损坏文件打不开则另报解析失败。复杂表格的阅读顺序我当时没做好，只做了几份常见文件的人工检查。

### 第 2 题（记录 #5）

你提到复杂表格的阅读顺序当时没做好，只做了几份常见文件的人工检查。能否举一个具体例子，说明你当时如何判断阅读顺序出了问题，以及后来有没有尝试过改进或验证？

回答：

【虚构候选人的模拟回答】有一份双栏 PDF，直接提取后左栏一句后面接了右栏一句，摘要把两段经历串在了一起。我是拿原页面和提取文本逐行对照才发现的。后来试过按文本块坐标排序，对那份样本有改善，但带跨栏标题的文件仍会错，所以没有把它当通用解决方案上线。我只加了解析文本预览让用户检查，完整的版面测试集没来得及做。如果现在重做，我会先收集获得授权的几类样本，再比较提取结果，不会声称换个库就全部解决。

模型观察：

本轮回答针对上一轮追问，给出了双栏 PDF 阅读顺序错乱的具体案例：直接提取后左栏一句接了右栏一句，导致摘要把两段经历串在一起；通过原页面与提取文本逐行对照发现；尝试按文本块坐标排序，对样本有改善但带跨栏标题的文件仍会错，因此未作为通用方案上线；只加了解析文本预览供用户检查，未完成完整版面测试集；并给出重做思路（先收集获得授权的几类样本再比较提取结果，不声称换库即可全部解决）。这些内容为“简历文件解析与异常处理”能力提供了较具体的线索，但仍是候选人自述，未经外部验证。

本轮引用：

【虚构候选人的模拟回答】有一份双栏 PDF，直接提取后左栏一句后面接了右栏一句，摘要把两段经历串在了一起。我是拿原页面和提取文本逐行对照才发现的。后来试过按文本块坐标排序，对那份样本有改善，但带跨栏标题的文件仍会错，所以没有把它当通用解决方案上线。我只加了解析文本预览让用户检查，完整的版面测试集没来得及做。如果现在重做，我会先收集获得授权的几类样本，再比较提取结果，不会声称换个库就全部解决。

### 第 3 题（记录 #6）

你提到处理过模型调用超时。请描述一次具体的超时场景：当时接口的表现是什么，你如何设置超时和重试，最终如何向调用方反馈结果？

回答：

【虚构候选人的模拟回答】当时有一份比较长的资料，摘要请求一直没有返回，页面就一直转圈。我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。第一次做得比较简单，超时就返回可重试的错误，保留原文件和解析文本，不把空字符串存成成功摘要。我没有直接无限重试，后来对限流和临时服务错误最多重试一次。用户再次点摘要可能重复花费，这个服务端幂等当时没做完整，是已知欠缺。对鉴权错误不会重试，也不会把上游完整错误和密钥展示给用户。

模型观察：

本轮回答针对当前问题（模型调用超时场景、超时与重试设置、如何向调用方反馈），给出了较具体的实现线索：摘要请求长时间未返回、页面持续转圈；后端补充连接超时约 5 秒、读取超时约 40 秒，并主动说明具体数字记不准；首次实现为超时返回可重试错误，保留原文件和解析文本，不把空字符串存成成功摘要；未做无限重试，对限流和临时服务错误最多重试一次；主动承认服务端幂等未做完整，用户再次点击摘要可能重复花费；对鉴权错误不重试，也不向上游展示完整错误和密钥。这些内容为“大模型 API 接入与超时处理”能力提供了直接线索，但均为候选人自述，未经外部验证。

本轮引用：

【虚构候选人的模拟回答】当时有一份比较长的资料，摘要请求一直没有返回，页面就一直转圈。我在后端补了连接超时和读取超时，大概是连接 5 秒、读取 40 秒，具体数字现在记不准。第一次做得比较简单，超时就返回可重试的错误，保留原文件和解析文本，不把空字符串存成成功摘要。我没有直接无限重试，后来对限流和临时服务错误最多重试一次。用户再次点摘要可能重复花费，这个服务端幂等当时没做完整，是已知欠缺。对鉴权错误不会重试，也不会把上游完整错误和密钥展示给用户。

### 第 4 题（记录 #7）

在设备借用管理系统中你使用了 FastAPI 和 SQLite。请讲一下借还接口涉及的表结构，以及你如何保证同一设备不会被同时借出两次？

回答：

【虚构候选人的模拟回答】我设计了设备表和借用记录表，设备有编号和可借状态，借用记录有设备、借用人、借出时间和归还时间。原来的实现是先查可借再写记录，现在想起来这在并发下不可靠；20 人使用时没遇到，不代表没有问题。如果改进，我会在事务里做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任何一步失败都回滚。这样两个请求不能都成功。我还没实际写过这个并发测试，需要用两个独立连接同时发请求验证。竞赛原型也是较简单的版本，不能用那个奖项证明我已经解决了生产并发。

模型观察：

本轮回答针对当前问题（设备借还接口的表结构，以及如何保证同一设备不会被同时借出两次），给出了较具体的实现线索：设计了设备表和借用记录表，设备含编号和可借状态，借用记录含设备、借用人、借出时间和归还时间；原实现为先查可借再写记录，候选人主动指出这在并发下不可靠，并说明 20 人使用时未遇到不代表没有问题；提出改进方案为在事务中做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任一步失败则回滚，使两个请求不能都成功；同时主动承认尚未实际编写该并发测试，需用两个独立连接同时发请求验证；并明确竞赛原型较简单，不能用该奖项证明已解决生产并发。这些内容为“SQLite 数据持久化”能力提供了直接线索，但均为候选人自述，未经外部验证。

本轮引用：

【虚构候选人的模拟回答】我设计了设备表和借用记录表，设备有编号和可借状态，借用记录有设备、借用人、借出时间和归还时间。原来的实现是先查可借再写记录，现在想起来这在并发下不可靠；20 人使用时没遇到，不代表没有问题。如果改进，我会在事务里做带条件的更新，只在设备仍可借时改成借出，检查受影响行数，再插入借用记录，任何一步失败都回滚。这样两个请求不能都成功。我还没实际写过这个并发测试，需要用两个独立连接同时发请求验证。竞赛原型也是较简单的版本，不能用那个奖项证明我已经解决了生产并发。

### 第 5 题（记录 #8）

假设你要为面试官交付一个可在 Windows 本地启动的 AI 面试辅助程序，包含 FastAPI 服务、SQLite 数据库和 DeepSeek 接入。你会如何设计启动方式、配置管理和备份说明？

回答：

【虚构候选人的模拟回答】我没有独立做过 Windows 安装包，会先在一台没有开发环境的电脑做最小启动验证，再承诺时间。我的方案是前端预先构建，随程序交付；后端只监听本机，提供启动和停止入口，启动成功再打开浏览器。程序、数据、日志分目录，甲方在设置页填自己的 Key，不把我的密钥或测试简历带过去。备份要同时考虑数据库和原文件，第一版可以先要求关闭程序后备份，并实际恢复一次。升级前保留备份。我还会让面试官试操作：如果他没时间逐字记回答，就要区分他的摘要和候选人原话，报告不能把摘要伪装成逐字引用。安装工具具体选哪种，我需要试验后再决定。

模型观察：

本轮回答针对当前问题（Windows 本地启动的 AI 面试辅助程序的启动方式、配置管理和备份说明），给出了较具体的交付思路线索：未独立做过 Windows 安装包，会先在一台没有开发环境的电脑做最小启动验证再承诺时间；前端预先构建随程序交付；后端只监听本机，提供启动和停止入口，启动成功再打开浏览器；程序、数据、日志分目录；甲方在设置页填自己的 Key，不携带开发方密钥或测试简历；备份同时考虑数据库和原文件，第一版可要求关闭程序后备份并实际恢复一次；升级前保留备份；安装工具具体选型需试验后再决定。此外还提到让面试官试操作，以及区分面试官摘要与候选人原话、报告不能把摘要伪装成逐字引用。这些内容为“本地部署与交付”能力提供了直接线索，但均为候选人自述，未经外部验证；安装工具选型、最小启动验证是否实际执行等仍属未知。

本轮引用：

【虚构候选人的模拟回答】我没有独立做过 Windows 安装包，会先在一台没有开发环境的电脑做最小启动验证，再承诺时间。我的方案是前端预先构建，随程序交付；后端只监听本机，提供启动和停止入口，启动成功再打开浏览器。程序、数据、日志分目录，甲方在设置页填自己的 Key，不把我的密钥或测试简历带过去。备份要同时考虑数据库和原文件，第一版可以先要求关闭程序后备份，并实际恢复一次。升级前保留备份。我还会让面试官试操作：如果他没时间逐字记回答，就要区分他的摘要和候选人原话，报告不能把摘要伪装成逐字引用。安装工具具体选哪种，我需要试验后再决定。